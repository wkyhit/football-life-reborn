# Enhanced UI design contract — compatibility pointer

The approved, app-wide Enhanced design system now lives at the repository root in [`design.md`](../../../design.md). Its companion sources are [`tokens.css`](../../../tokens.css) and [`tokens.json`](../../../tokens.json).

This path remains only because earlier phases and audit evidence reference it. It does not define an independent palette, type scale, spacing system, component state, or page exception.

Classic remains governed by `src/ui/classic/tokens.css` and is outside the root Enhanced system.
